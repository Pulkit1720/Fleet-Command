# Build / dependency artifacts (Vercel rebuilds these)
node_modules
.next
.vercel

# Local env files — provide these via Vercel project env vars instead
.env
.env.*

# iCloud "Desktop & Documents" conflict-copy duplicates, e.g. "middleware 3.ts",
# "Dockerfile 2". These are junk and have broken the production build before.
* [0-9]
* [0-9].*

# Tests / tooling not needed in the deployed bundle
**/__tests__
vitest.config.ts
