{
  "name": "fleetcommand-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web-admin run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:mobile": "npm --prefix mobile-app start",
    "dev:web": "npm --prefix web-admin run dev",
    "test": "npm --prefix backend run test && npm --prefix web-admin run test && npm --prefix mobile-app run test",
    "test:coverage": "npm --prefix backend run test:coverage"
  }
}
